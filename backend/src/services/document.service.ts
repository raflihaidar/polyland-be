import { prisma } from "../config/prisma";
import { AppError } from "../utils/error";
import fs from "fs";
import path from "path";

export const generateOwnershipTransferApp = async (data : any) => {
    return data
}